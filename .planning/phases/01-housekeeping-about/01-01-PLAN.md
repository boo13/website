---
phase: 01-housekeeping-about
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - case_study_wyatt.html
  - src/main-wyatt.js
  - src/styles/wyatt.css
  - vite.config.js
  - index2.html
  - src/main.js
  - src/sections/featured-work.js
  - src/styles/index2.css
autonomous: true

must_haves:
  truths:
    - "case_study_wyatt.html loads in browser and displays the Wyatt Earp parallax section with both layer images and text content visible"
    - "Wyatt Earp section no longer appears in index2.html"
    - "index2.html loads without console errors after extraction"
    - "No orphaned parallax styles remain in index2.css that reference removed HTML"
  artifacts:
    - path: "case_study_wyatt.html"
      provides: "Standalone Wyatt Earp case study page"
      contains: "parallax-section"
    - path: "src/main-wyatt.js"
      provides: "Entry point for case study page"
      contains: "initFeaturedWork"
    - path: "src/styles/wyatt.css"
      provides: "Extracted parallax styles for case study page"
      contains: "parallax-container"
  key_links:
    - from: "case_study_wyatt.html"
      to: "src/main-wyatt.js"
      via: "script module tag"
      pattern: 'src="/src/main-wyatt.js"'
    - from: "src/main-wyatt.js"
      to: "src/sections/featured-work.js"
      via: "import initFeaturedWork"
      pattern: "import.*initFeaturedWork"
    - from: "vite.config.js"
      to: "case_study_wyatt.html"
      via: "rollupOptions.input entry"
      pattern: "case_study_wyatt"
---

<objective>
Extract the Wyatt Earp parallax section from index2.html into a standalone case_study_wyatt.html page, then clean up index2.html so it has no orphaned references to the removed section.

Purpose: HOUSE-01 — the Wyatt Earp section is being moved to its own case study page so index2.html focuses on the new About section narrative. This clears the way for Plan 02 to insert the About slides between Hero and Featured Work Gallery.

Output: A working case_study_wyatt.html page with the parallax rack-focus effect, and a cleaned-up index2.html with no Wyatt Earp remnants.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@index2.html
@vite.config.js
@src/main.js
@src/sections/featured-work.js
@src/styles/index2.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create standalone case_study_wyatt.html with entry point and extracted styles</name>
  <files>
    case_study_wyatt.html
    src/main-wyatt.js
    src/styles/wyatt.css
    vite.config.js
  </files>
  <action>
  1. Create `case_study_wyatt.html` at the repo root with:
     - Full HTML5 boilerplate (DOCTYPE, html lang="en", charset, viewport meta)
     - Title: "Wyatt Earp and The Cowboy War - Randy Counsman"
     - Same Typekit font link as index2.html: `https://use.typekit.net/bnp0hyp.css`
     - Critical inline styles matching index2.html (dark background `#0a0a0a`, no flash)
     - The exact Wyatt Earp `<section id="wyatt-earp" class="parallax-section">` block from index2.html lines 113-157, copied verbatim
     - Script tag: `<script type="module" src="/src/main-wyatt.js"></script>`
     - Optional: a simple back link to index2.html at the top

  2. Create `src/main-wyatt.js` as the entry point:
     - Import `./styles/wyatt.css`
     - Import `initFeaturedWork` from `./sections/featured-work.js` (this is the existing parallax animation function — it works by querying `.parallax-section` in the DOM, not tied to index2.html specifically)
     - Call `initFeaturedWork()` on DOMContentLoaded (same pattern as other entry points)
     - Handle cleanup on pagehide

  3. Create `src/styles/wyatt.css` containing:
     - CSS custom properties that the parallax styles depend on (copy from index2.css: `--color-nearblack`, `--color-offwhite`, `--ff-display`, `--container-padding`, etc. — check the `:root` block in index2.css)
     - Base body/html styles (background: #0a0a0a, margin: 0, color scheme)
     - ALL parallax-related CSS rules extracted from index2.css:
       - `.parallax-section` (line 240+)
       - `.parallax-container` (line 246+)
       - `.parallax-layer` (line 254+)
       - `.parallax-bg`, `.parallax-fg` (lines 263+, 268+)
       - `.parallax-text` and all its children (lines 273-321)
       - `.parallax-fade`, `.parallax-bg-fade`, `.parallax-fg-fade` (lines 323-352)
       - Responsive rules from media queries: tablet (1024px) `.parallax-text .quotes`, mobile (768px) `.parallax-section`, `.parallax-text`, `.parallax-text .project-title`, `.parallax-text .quote`
     - The cursor styles (`.parallax-section { cursor: none; }` lines 80-82, 86-88) — include but ALSO add a regular cursor fallback since the case study page won't have the custom cursor component

  4. Add `case_study_wyatt.html` to `vite.config.js` rollupOptions.input:
     ```javascript
     wyatt: resolve(import.meta.dirname, 'case_study_wyatt.html'),
     ```
     Add it after the existing entries.

  IMPORTANT: The existing `src/sections/featured-work.js` file is NOT renamed or moved — it stays where it is and serves both the case study page and potentially future use. The file queries for `.parallax-section` in the DOM, so it works regardless of which HTML page loads it.
  </action>
  <verify>
  Run `npm run build` — should complete without errors and include case_study_wyatt.html in dist/.
  Open case_study_wyatt.html in the browser via `npm run dev` — the parallax rack-focus effect should work with both layer images visible and text content readable.
  </verify>
  <done>case_study_wyatt.html renders the Wyatt Earp parallax section with working rack-focus animation, both layer images load from public/images/, text content is visible, and the page is included in Vite's build output.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Wyatt Earp section from index2.html and clean up orphaned references</name>
  <files>
    index2.html
    src/main.js
    src/styles/index2.css
  </files>
  <action>
  1. In `index2.html`, remove the entire Wyatt Earp section block (lines 108-157, from the comment `SECTION 2: WYATT EARP` through the closing `</section>` tag). Leave a blank line or comment like `<!-- About section will be inserted here by Plan 02 -->` to mark where the About section goes.

  2. In `src/main.js`:
     - Remove the import of `initFeaturedWork` from `./sections/featured-work.js` (line 3)
     - Remove the call to `initFeaturedWork()` (line 13)
     - Do NOT delete `src/sections/featured-work.js` itself — it's still used by main-wyatt.js

  3. In `src/styles/index2.css`, remove ALL parallax-related styles that are now exclusively in wyatt.css:
     - Lines 80-82: `.parallax-section { cursor: none; }` — remove the `.parallax-section` selectors from the combined rule (keep `.hero-section` cursor rules)
     - Lines 86-88: `.parallax-section a, .parallax-section button { cursor: none; }` — same, remove parallax selectors only
     - Lines 240-352: The entire parallax section block (`.parallax-section` through `.parallax-fg-fade`)
     - Responsive media query rules referencing parallax:
       - At 1024px: `.parallax-text .quotes` (line 811)
       - At 768px: `.parallax-section`, `.parallax-text`, `.parallax-text .project-title`, `.parallax-text .quote` (lines 860-875)
     - Update the section comment numbering if desired (Gallery becomes Section 2, Credits becomes Section 3, etc.)

  4. Verify no other files in `src/` reference `.parallax-section` class that would break. The `custom-cursor.js` file references parallax — check if it needs updating. If custom-cursor.js adds cursor behavior to `.parallax-section`, remove that reference since the section no longer exists in index2.html.

  IMPORTANT: Do NOT remove `src/sections/featured-work.js` or `src/styles/wyatt.css` — those are used by the case study page.
  </action>
  <verify>
  Run `npm run dev` and load index2.html — page should load without console errors.
  The Wyatt Earp section should not appear anywhere on index2.html.
  Run `npm run build` — should complete without errors.
  Run `npm run lint` — no import/reference errors.
  </verify>
  <done>index2.html loads cleanly without the Wyatt Earp section. No console errors, no orphaned CSS rules referencing parallax classes, no orphaned JS imports. The page flows directly from Hero to Gallery (temporarily, until About slides are inserted).</done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. `npm run dev` — index2.html loads without console errors, no Wyatt Earp section visible
3. `npm run dev` — case_study_wyatt.html loads with working parallax effect, both images visible
4. `npm run lint` passes
5. No references to `.parallax-section` remain in index2.css
6. `src/sections/featured-work.js` still exists (used by case study page)
</verification>

<success_criteria>
- case_study_wyatt.html is a working standalone page with the Wyatt Earp parallax rack-focus effect
- index2.html has zero Wyatt Earp content and loads without errors
- Build passes, lint passes
- No orphaned styles or scripts in index2.html's dependency chain
</success_criteria>

<output>
After completion, create `.planning/phases/01-housekeeping-about/01-01-SUMMARY.md`
</output>
