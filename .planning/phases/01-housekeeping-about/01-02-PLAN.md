---
phase: 01-housekeeping-about
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index2.html
  - src/sections/about-slides.js
  - src/styles/about-slides.css
  - src/main.js
  - src/animations/scroll-defaults.js
autonomous: true

must_haves:
  truths:
    - "Three full-viewport About slides are visible between Hero and Gallery sections in index2.html"
    - "Each slide fills the entire viewport height (100svh with 100vh fallback)"
    - "Slide 1 shows 'I've worked on' intro text and 'Storytelling that goes BIG...' headline over a background image"
    - "Slide 2 shows 'as well as' intro text, 'Storytelling that fits in your hand...' headline, and 4 phone mockup containers"
    - "Slide 3 shows 'This is me...' handwritten text on dark background"
    - "The About section init function is called from main.js and wrapped in gsap.context()"
  artifacts:
    - path: "src/sections/about-slides.js"
      provides: "About section initialization with gsap.context scaffold"
      exports: ["initAboutSlides"]
      min_lines: 30
    - path: "src/styles/about-slides.css"
      provides: "Full-viewport slide layout CSS for all 3 About slides"
      contains: "100svh"
      min_lines: 80
  key_links:
    - from: "src/main.js"
      to: "src/sections/about-slides.js"
      via: "import initAboutSlides"
      pattern: "import.*initAboutSlides.*about-slides"
    - from: "src/main.js"
      to: "initAboutSlides()"
      via: "function call in init()"
      pattern: "initAboutSlides"
    - from: "src/sections/about-slides.js"
      to: "src/animations/scroll-defaults.js"
      via: "import gsap and ScrollTrigger"
      pattern: "import.*scroll-defaults"
    - from: "index2.html"
      to: "src/sections/about-slides.js"
      via: "DOM class queries (.about-slides-section)"
      pattern: "about-slide"
---

<objective>
Build the HTML structure and CSS layout for all 3 About section slides, plus a JS scaffold that initializes the section with gsap.context(). No complex animations yet — just the structural foundation that Plans 03 and 04 will animate.

Purpose: ABOU-01 — establish the full-viewport slide sequence between Hero and Gallery. This plan creates the DOM elements, typography, grid containers, phone mockup placeholders, and viewport-filling CSS that the animation plans depend on.

Output: Three visible full-viewport slides in index2.html with correct typography hierarchy, placeholder content, and a section init function wired into main.js. Scrolling through the page shows the three slides stacked vertically at full viewport height.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-housekeeping-about/01-CONTEXT.md
@.planning/phases/01-housekeeping-about/01-RESEARCH.md
@index2.html
@src/main.js
@src/sections/landing.js
@src/animations/scroll-defaults.js
@src/animations/text-mask-rise.js
@src/styles/index2.css
@src/config.js
@ref/Artboard 1.png
@ref/WORK - Digital.png
@ref/Artboard 3.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add About section HTML to index2.html with all 3 slide structures</name>
  <files>
    index2.html
  </files>
  <action>
  Insert a new About section in index2.html BETWEEN the Hero section (closing `</section>` after line 106) and the Gallery section (the `SECTION 3: FEATURED WORKS GALLERY` comment). If Plan 01 has already removed the Wyatt Earp section, insert right after Hero. If Plan 01 hasn't run yet, insert after the Wyatt Earp section (it will be removed separately).

  The About section structure:

  ```html
  <!-- =====================================================
       SECTION: ABOUT SLIDES
       Full-viewport scroll-driven narrative slides
       "I've worked on..." -> "as well as..." -> "This is me..."
  ====================================================== -->
  <section class="about-slides-section">

    <!-- Slide 1: "Storytelling that goes BIG" -->
    <div class="about-slide slide-goes-big">
      <div class="slide-bg">
        <!-- Background: dam burst still image, will be zoomed in initially -->
        <div class="grid-container">
          <!-- Center hero image (the dam burst still) -->
          <div class="grid-item grid-hero">
            <img src="images/portfolio/MWBA.jpg" alt="" loading="lazy">
          </div>
          <!-- Surrounding grid items (dramatic shots from previous work) -->
          <!-- Use existing portfolio images as placeholders; these get revealed on zoom-out -->
          <div class="grid-item"><img src="images/shows/CapsizedBloodInTheWater_TitleCard.webp" alt="" loading="lazy"></div>
          <div class="grid-item"><img src="images/portfolio/Pope.jpg" alt="" loading="lazy"></div>
          <div class="grid-item"><img src="images/Wyatt_Layer01_v01.png" alt="" loading="lazy"></div>
          <div class="grid-item"><img src="images/portfolio/MWBA.jpg" alt="" loading="lazy"></div>
          <div class="grid-item"><img src="images/shows/CapsizedBloodInTheWater_TitleCard.webp" alt="" loading="lazy"></div>
          <div class="grid-item"><img src="images/portfolio/Pope.jpg" alt="" loading="lazy"></div>
          <div class="grid-item"><img src="images/Wyatt_Layer02_v01.png" alt="" loading="lazy"></div>
          <div class="grid-item"><img src="images/portfolio/MWBA.jpg" alt="" loading="lazy"></div>
        </div>
      </div>
      <div class="slide-content">
        <p class="slide-intro handwritten">I've worked on</p>
        <h2 class="slide-headline">Storytelling<br>that goes <em>BIG</em>...</h2>
      </div>
    </div>

    <!-- Slide 2: "Storytelling that fits in your hand" -->
    <div class="about-slide slide-in-your-hand">
      <div class="slide-content">
        <p class="slide-intro handwritten">as well as</p>
        <h2 class="slide-headline">Storytelling<br>that fits in your hand...</h2>
      </div>
      <div class="phone-mockups">
        <!-- Layout matches WORK-Digital.png: 3 smaller phones left/center, 1 large phone right -->
        <div class="phone-mockup phone-sm phone-1">
          <div class="phone-frame">
            <div class="phone-screen">
              <!-- Placeholder: will be replaced with actual mockup images -->
              <div class="phone-placeholder"></div>
            </div>
          </div>
        </div>
        <div class="phone-mockup phone-sm phone-2">
          <div class="phone-frame">
            <div class="phone-screen">
              <div class="phone-placeholder"></div>
            </div>
          </div>
        </div>
        <div class="phone-mockup phone-sm phone-3">
          <div class="phone-frame">
            <div class="phone-screen">
              <div class="phone-placeholder"></div>
            </div>
          </div>
        </div>
        <div class="phone-mockup phone-lg phone-4">
          <div class="phone-frame">
            <div class="phone-screen">
              <div class="phone-placeholder"></div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Slide 3: "This is me" -->
    <div class="about-slide slide-this-is-me">
      <div class="slide-content">
        <p class="slide-intro handwritten this-is-me-text">This is me...</p>
        <!-- Placeholder container for future portrait + particle/Gaussian splat effect -->
        <div class="portrait-placeholder">
          <!-- Will eventually contain canvas/WebGL for particle effect -->
          <!-- For now, empty or with a subtle placeholder -->
        </div>
      </div>
    </div>

  </section>
  ```

  IMPORTANT:
  - Images reference paths like `images/portfolio/MWBA.jpg` — these resolve to `public/images/portfolio/MWBA.jpg` via Vite's public directory. Verify these image paths exist. If not, use whatever portfolio images ARE available in `public/images/`.
  - The `.handwritten` class will use a script/handwritten font (from Typekit or a web-safe fallback). Check what fonts are available from the Typekit link.
  - The grid should have 9 items total (3x3 grid) with the center item being the "hero" dam burst image.
  - Phone mockups use placeholder divs for now — the user said mockup image assets will be provided later.
  </action>
  <verify>
  Open index2.html in the browser via `npm run dev`. Three tall sections should be visible between Hero and Gallery when scrolling. Each slide should fill the viewport height. Text content should be visible (even if not yet animated).
  </verify>
  <done>index2.html contains three full-viewport About slides between Hero and Gallery with correct HTML structure, placeholder images in the grid, phone mockup containers, and "This is me" text.</done>
</task>

<task type="auto">
  <name>Task 2: Create about-slides.css layout and about-slides.js scaffold, wire into main.js</name>
  <files>
    src/styles/about-slides.css
    src/sections/about-slides.js
    src/main.js
    src/animations/scroll-defaults.js
  </files>
  <action>
  1. Create `src/styles/about-slides.css` with the full-viewport slide layout:

  **Base slide styles:**
  - `.about-slides-section` — `position: relative; width: 100%; background: var(--color-nearblack);`
  - `.about-slide` — full viewport height using `height: 100vh; height: 100svh; min-height: 100svh;` with `position: relative; display: flex; align-items: center; justify-content: center; overflow: hidden; padding: 2rem;` and `@supports not (height: 100svh)` fallback
  - Each slide gets a dark background by default: `background: #0a0a0a;`

  **Typography — two-font system:**
  - `.slide-intro.handwritten` — Use a script/handwritten font. Check Typekit link (bnp0hyp) for available fonts; if a handwritten/script font is available use it, otherwise use a web-safe cursive stack like `'Caveat', 'Segoe Script', cursive`. Size: `clamp(1.5rem, 3vw, 2.5rem)`. Color: `rgba(247, 244, 240, 0.85)`. Should feel casual, personal.
  - `.slide-headline` — Bold serif using `var(--ff-display)` (already defined in index2.css root). Size: `clamp(3rem, 8vw, 7rem)`. Font-weight: 700. Color: `var(--color-offwhite)`. Line-height: 1.05. The `em` inside gets slightly different styling (underline or italic for emphasis on "BIG").

  **Slide 1 ("Goes BIG") specific:**
  - `.slide-goes-big` — `overflow: hidden;` (critical: grid zooms within bounds initially)
  - `.slide-bg` — `position: absolute; inset: 0; z-index: 1;`
  - `.grid-container` — CSS Grid, 3 columns x 3 rows. Size: `width: 175%; height: 175%;` (oversized, spills off all edges). Position: `position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);` with `transform-origin: center center;`. Initially will be scaled up to 3x by JS (zoomed into center image), but CSS sets the natural oversized state.
  - `.grid-item` — `overflow: hidden;` with images `width: 100%; height: 100%; object-fit: cover;`
  - `.grid-hero` — center item, slightly larger or same size
  - `.slide-goes-big .slide-content` — Positioned over grid with `position: relative; z-index: 2; text-align: left; padding-left: 8vw;` to match ref image (text is left-aligned)

  **Slide 2 ("In your hand") specific:**
  - `.slide-in-your-hand` — `flex-direction: column; align-items: flex-start; padding: 4rem 6vw;`
  - `.slide-in-your-hand .slide-content` — positioned top-left area of slide
  - `.phone-mockups` — Container for phones. Use CSS to position them matching the reference image (WORK-Digital.png): 3 smaller phones on the left/center area, 1 large phone on the right. Consider absolute positioning or a custom grid.
    - `.phone-sm` — smaller phones, roughly 180px wide on desktop
    - `.phone-lg` — large phone, roughly 320px wide on desktop, positioned right
    - `.phone-frame` — Phone device frame styling (rounded corners `border-radius: 2.5rem`, dark border, notch suggestion at top)
    - `.phone-screen` — `overflow: hidden; border-radius: 2rem; aspect-ratio: 9/19.5;` (iPhone proportions)
    - `.phone-placeholder` — `width: 100%; height: 100%; background: linear-gradient(135deg, #1a1a2e, #16213e);` as a placeholder gradient
  - Position phones at bottom of slide, text at top — matching the reference layout

  **Slide 3 ("This is me") specific:**
  - `.slide-this-is-me` — `background: #000; justify-content: flex-start; align-items: flex-start; padding: 20vh 8vw;`
  - `.this-is-me-text` — Larger than other intros: `clamp(2rem, 5vw, 4rem)`
  - `.portrait-placeholder` — `width: 300px; height: 400px; margin-top: 2rem;` (hidden or subtle for now, just reserves the space for future portrait effect)

  **Responsive (mobile-first adjustments):**
  - At 768px: reduce headline sizes, stack phone mockups vertically, reduce padding

  2. Create `src/sections/about-slides.js`:
  ```javascript
  import { gsap, ScrollTrigger } from '../animations/scroll-defaults.js';

  export function initAboutSlides() {
    const section = document.querySelector('.about-slides-section');
    if (!section) return () => {};

    const prefersReducedMotion = window.matchMedia(
      '(prefers-reduced-motion: reduce)'
    ).matches;

    if (prefersReducedMotion) {
      // Show all content immediately, no animations
      gsap.set(section.querySelectorAll('.slide-content'), { opacity: 1 });
      return () => {};
    }

    const ctx = gsap.context(() => {
      // Animation code will be added by Plans 03 and 04
      // For now, just ensure content is visible
      ScrollTrigger.refresh();
    }, section);

    return () => {
      ctx.revert();
    };
  }
  ```

  3. Update `src/main.js`:
  - Add import: `import { initAboutSlides } from './sections/about-slides.js';`
  - Add CSS import: `import './styles/about-slides.css';`
  - Call `initAboutSlides()` in the `init()` function, storing the cleanup:
    ```javascript
    const cleanupAboutSlides = initAboutSlides();
    ```
  - Add cleanup to the pagehide handler:
    ```javascript
    if (typeof cleanupAboutSlides === 'function') cleanupAboutSlides();
    ```

  4. In `src/animations/scroll-defaults.js`, register SplitText if not already registered (needed for future text animations, but register now so the import is available):
  - Add `import SplitText from 'gsap/SplitText';` and `gsap.registerPlugin(SplitText);` — BUT check if text-mask-rise.js already registers it independently. If so, consider whether scroll-defaults should centralize registration. If text-mask-rise.js handles its own SplitText registration, leave scroll-defaults as-is for now.

  NOTE: The existing `initAbout()` function in `src/sections/about.js` handles the old stats-based About section (lines 296-354 of index2.html). That old About section still exists in index2.html and will be replaced or updated in a later phase. Do NOT remove the old about.js or its import — they're separate from the new about-slides section.
  </action>
  <verify>
  Run `npm run dev` and load index2.html. Scroll past the Hero section. Three full-viewport slides should appear:
  1. "Storytelling that goes BIG" with a background grid of images and left-aligned text
  2. "Storytelling that fits in your hand" with text and phone mockup outlines
  3. "This is me..." on a dark background
  Each slide fills the viewport height. No console errors. `npm run lint` passes.
  </verify>
  <done>About slides have full-viewport CSS layout, visible typography with two-font system (handwritten intros + bold serif headlines), phone mockup placeholder frames, grid container for images, and a working section init function wired into main.js. All three slides are scrollable and fill the viewport.</done>
</task>

</tasks>

<verification>
1. `npm run dev` — index2.html shows three full-viewport About slides between Hero and Gallery
2. Each slide fills exactly one viewport height (100svh)
3. Slide 1 shows grid of images behind "Storytelling that goes BIG" text
4. Slide 2 shows phone mockup outlines with "Storytelling that fits in your hand" text
5. Slide 3 shows "This is me..." text on dark background
6. Typography uses two distinct font treatments (handwritten intro + bold serif headline)
7. `npm run build` passes
8. `npm run lint` passes
9. No console errors
</verification>

<success_criteria>
- Three full-viewport About slides visible in index2.html between Hero and Gallery
- Each slide has correct content structure matching the reference designs
- Section init function exists and is called from main.js with proper cleanup
- CSS uses 100svh with 100vh fallback for mobile compatibility
- Phone mockup containers ready for future image content
- Grid container oversized (175% viewport) and ready for zoom animation
</success_criteria>

<output>
After completion, create `.planning/phases/01-housekeeping-about/01-02-SUMMARY.md`
</output>
