# Portfolio variant passwords — one per variant
# Copy this file to .env and set real passwords (never commit .env)
#
# After setting a password, run:
#   just portfolio-encrypt <slug>
# to generate the encrypted data file for deployment.
#
PORTFOLIO_0626_PASSWORD=
PORTFOLIO_DESIGN_PASSWORD=
